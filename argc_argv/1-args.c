#include <stdio.h>

/**
 * main - Enter point
 * @argc: Argument count
 * @argv: Argument massiv
 *
 * Return: Always 0
 */
int main(int argc, char *argv[])
{
	int i;
	(voi)argv;

	for (i = 0; i <= argc; i++)
	{
		printf("%s\n", i - 1);
	}
	return (0);
}
